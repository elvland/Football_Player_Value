import sys

# Replace 'my_env' with the name of your Anaconda environment
env_name = 'my_env'

# Get the path to the interpreter for the specified environment
interpreter_path = sys.executable

print("Interpreter path for environment '{}' is: {}".format(env_name, interpreter_path))

#%%
